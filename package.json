{
  "name": "scopegraph",
  "version": "0.0.0-dev",
  "private": true,
  "description": "Static semantic analysis for AI agent authority.",
  "type": "module",
  "bin": {
    "scopegraph": "./dist/cli/scan.js"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test tests/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "^22.17.0"
  }
}
